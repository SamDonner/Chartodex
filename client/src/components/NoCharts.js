import React from 'react';
import HomeImage  from '../assets/HomeImage.jpg';

const NoCharts = (props) => {
  return (   
    <div className="showcase_wrap center">
      
          <h4 className="showcase_wrap_text"><strong>No charts yet</strong></h4>
          <h6>Add charts to your watchlist by using the menu on the left or from the main menu at the top of the page </h6>
          <div className="nav-hamburger mt-2">HAPPY TRADING!!!</div>
     
    </div>
  );
};

export default NoCharts;